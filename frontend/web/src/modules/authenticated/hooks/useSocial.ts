import { useState } from 'react'

function useSocial(postId: number) {
  const addComment = (comment: string) => {
    // send the post via post endpoint
  }

  const addLike = () => {
    // add like via endpoint
  }

  const removeLike = () => {
    // do somehting
  }
}
